def longestCommonPrefix( strs):
        prefix = ''
        previous = strs[1][1]
        print(previous)
        # for c in strs:
        #     check = check and (c[i] == previous) 

longestCommonPrefix(["dog", "racecar", "car"])
